import styled from "styled-components";

export const StyledCard = styled.div`
  background-color: #0a001e;
  border: 2px solid #30363d;
  box-shadow: 0px 0px 16px 2px rgba(47, 0, 142, 0.93);
  width: fit-content;
  border-radius: 1.3rem;
  padding: 1.5rem 4rem;
  display: flex;
  align-items: center;

  .repo_name {
    color: #fff;
    font-weight: bold;
  }
`;
